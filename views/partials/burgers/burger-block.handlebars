<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newstatus="{{devoured}}">
		{{#if devoured}}MUNCH MUNCH!{{else}}FRESH FROM THE GRILL!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
